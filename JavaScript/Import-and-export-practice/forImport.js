// For Import
import { forAdd } from "./forExport.js";

let userNumber1 = +prompt("Enter the first number:");
let userNumber2 = +prompt("Enter the second number:");

console.log(forAdd(userNumber1, userNumber2));